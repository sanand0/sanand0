Alex: Hello and welcome to Anand's Weekly Codecast for the week of 20 September 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week. Let's start with Anand's talks and publishing workflow — he reorganized how he manages his talk pages and automated blog posts.
Maya: Right — he moved to a single source of truth for talks, added generator scripts and tests, and made the catalog generation deterministic. In plain language: instead of hand-editing a long index or pasting files, he now manages talk metadata in one config file and runs a build that spits out the website parts.
Alex: Why that matters practically is simple: one place to edit, fewer mistakes, and predictable output. If you add a talk or an image in the config, the site regenerates without accidentally stomping hand-edited pages.
Maya: Non-obvious takeaway: treat generated files as ephemeral. Edit the canonical JSON or YAML, not the generated README or index.html. That makes automation safe — and Anand even added small rules like a primary link field so the system knows which link becomes the title.
Alex: Also, he added programmatic images and resource icons, tests to render example posts, and an actionable command to rebuild. If you give your content a consistent shape, you can reliably make scripts produce polished posts.

Maya: Next up — Anand published new LLM evaluation work that’s pretty interesting. He ran a focused benchmark of a cheap decision model called Jev against several frontier language models on a 77-intent support-routing task.
Alex: Short version: Jev is very low-cost but a bit less accurate. It hits roughly mid-70% accuracy on that task; some other models were in the high-70s to low-80s. So Jev might be attractive for cost-sensitive uses, but it’s not yet a straight upgrade in quality.
Maya: The deeper part is his confidence experiments. He compared models’ stated confidence versus token log-probabilities — the number the model assigns to the first token it emits — and found an important pattern: raw logprobs are worse calibrated as literal probabilities, but they rank correctness better.
Alex: In plain terms: logprobs are better at ordering “which answers are likely right,” even if the number itself needs calibration. Anand froze routing cutoffs on development data and then ran a large prospective holdout. The logprob-based rule covered far more cases at the declared 5% error budget — roughly 65% coverage versus 41% for the model’s stated confidence — and that advantage held up on the untouched holdout.
Maya: Why this matters: if you’re automating routing or auto-accept decisions with an error budget, ranking signals that rank risk well can let you skip more work safely — but only if you validate thresholds on proper holdouts. Non-obvious: optimizing for discrimination (ranking) is different from optimizing for calibrated probability numbers; you need both, depending on the task.

Alex: On the blog front, Anand added a bunch of talk posts and refined many of his prompt fragments and "AI advice" notes.
Maya: He made editorial tweaks that are small on the surface — grammar, dash styles, paragraph rules — but important for downstream automation. Those fragments become inputs for scripts, so consistent punctuation and formatting reduce surprises.
Alex: Practically, he also added new posts — personal stories, TIL notes, and a set of deterministically generated talk posts that include dates, images, and links. Together with the talks tooling, this makes publishing repeatable.
Maya: Non-obvious takeaway: invest a little time making your style rules machine-friendly. It saves a lot of debugging later when automated scripts generate content.

Maya: Now, a classroom tool update: Anand improved the live workshop forms he runs from his laptop. He added a presenter control to show or hide questions, persisted that change back to the form YAML, and made the browser preserve your scroll position and focus during live updates.
Alex: That's a huge UX win. During workshops you can stage questions, press "Show," and the audience’s devices pick up the new question without everyone losing their place. He even added optimistic concurrency checks so you don't overwrite manual edits — the server returns a 409 if the config changed underneath you.
Maya: Why it matters: live forms are fragile in a room. Preserving viewport and selection avoids interruptions, and writing visibility changes atomically keeps the file safe. Non-obvious: when you let presenters mutate config in realtime, include a config_version check and atomic write to avoid races and surprises.

Alex: Anand also re-ran his quirky random-number experiment with newer models. The results are fun and telling: newer models still show strong, non-uniform preferences — one model loved 57, another favored 47 — and these preferences shift between models but persist.
Maya: He made the generator resumable and idempotent, saving after every successful response, so long runs can be resumed safely. Practical idea: when running many API calls for experiments, checkpoint after each response and skip already-done work on reruns. Saves money and time.

Maya: Tools got an update too: Anand added a ChatGPT-sidebar scraper bookmarklet that collects chat titles and URLs from the ChatGPT sidebar and copies them as Markdown or JSON.
Alex: That’s surprisingly handy if you want to archive or share a list of conversations. He built it to handle virtualized lists — i.e., scroll to reveal more items, and keep grabbing them — and added tests and fixtures.
Maya: Non-obvious: scrapers for modern web apps must handle lazy-rendered content and give users a way to accumulate entries while they scroll.

Alex: A few small but helpful fixes shipped as well. He added a link to a standalone visualization app for inspecting LLM log-probs and fixed an unsafe redirect by encoding the login redirect URL for the API proxy he runs.
Maya: He also allowed one trusted GitHub Pages origin for a login redirect so Pages-hosted prototypes can receive an AI Pipe token. These are little plumbing fixes but they make demos and single-file apps far less painful to use.

Maya: Anand also updated his weekly podcast generator repository and some podcast episode scripts — meta, because he uses automation to generate two-host scripts. He’s clearly iterating both on content and on tools that produce the content.
Alex: And finally, a quick note: he updated his LLM pricing/elo data and his TILs — housekeeping that keeps the public analyses up to date.

Maya: Okay, listener tips. I'll go first: if you publish talk notes or events, use a single source of truth (a config or JSON) and make generated pages transient. Then add a small "primary" link field so your generator knows which URL to show as the title.
Alex: Good one. How would you apply it in practice?
Maya: I’d export my event list from whatever calendar or CMS I use into a single JSON, run a generator to create index pages and per-event pages, and keep that JSON under version control so I can script safe edits.

Alex: My tip: when you design an automated review-or-skip rule for model outputs, validate it on a proper holdout and distinguish between ranking ability and calibration. Use a ranking signal (like logprob) to prioritize or defer review, but compute a calibrated probability if you need to give a literal confidence number.
Maya: How would you apply that?
Alex: I’d run a paired experiment on historical cases, pick a ranking cutoff that meets my error SLA on a development set, then freeze it and run on an untouched holdout to confirm coverage and observed error. If it drifts, tighten the process and include a human-in-the-loop until stable.

Maya: One more small practical tip from the liveform work: when your UI receives incremental updates, try to capture the visible anchor — which question or input is in view and which control has focus — and restore it after the DOM changes, including caret selection if possible.
Alex: I’d use that by default in any collaborative or live-update form I build. It’s amazing how much smoother a session feels when people don’t lose their typing spot.

Alex: That’s our roundup for Anand’s week — lots of engineering polish, reproducible experiments, and practical UX fixes.
Maya: Thanks for listening. We’ll be back next week with more highlights from Anand’s repos and experiments.
Alex: Bye for now!
Maya: See you next week!