Alex: Hello and welcome to Anand's Weekly Codecast for the week of 19 July 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week from Anand on his personal website and blog. He fixed a broken build by bumping the static-site tool, tightened up URL and slug handling, and made the 404 page into a usable search page. Why does that matter?
Maya: If your static site build fails, nothing gets published — visitors see an error. Updating the site generator to a newer stable release fixed a template runtime error. Practically, that means the site builds cleanly on CI and locally again.
Alex: Right, and the slug and URL fixes ensure links in the public site are consistent. Anand adjusted the logic that computes canonical URLs so pages like nested skills or pages keep the right path. That prevents dead links and makes search indexes accurate.
Maya: And the 404 change is neat: instead of a dead-end page, the 404 now embeds the same search component used elsewhere, pre-filled from the wrong URL. So if you mistype a path, the site will help you search for what you likely meant. Small UX change, big reduction in user friction.
Alex: He also added tests and a copying step so GitHub Pages will use the 404 at the site root — the subtle deployment bits that often trip people up.
Maya: Also notable: Anand published a bunch of "skills" — structured prompts and playbooks for different tasks like email replies, workshop follow-up, ideation, generating fake data, and a writing-style skill that codifies how he wants things written.
Alex: Those SKILL documents are basically reusable, human-readable instructions for agents — they make it much safer and more consistent when he asks AI tools to draft or act on his behalf.
Maya: Non-obvious takeaway: invest in a small set of high-quality, documented prompts or skills. They act like test-suite scaffolding for your interactions with AI — fewer surprises, better repeatable outcomes.

Alex: Quick listener tip from this segment: if you run a small static site, keep your build tool on a recent stable version and add a minimal smoke test in CI that checks the homepage and 404. Maya, how would you apply that?
Maya: I'd add a quick script that builds the site and then runs a tiny HTML check on a couple of pages; for 404, test that it includes the search component. That would catch regressions before they reach production.

Alex: Next up: Anand's machine-utility and dotfiles repo saw a flurry of practical work — a substantial rewrite of a browser-inspection tool, better Chrome/Edge tab helpers, and a new "read" tool for his local MCP server so agents can fetch binary files safely.
Maya: Let's unpack that. He consolidated an older script into a single "edge" command with subcommands like tabs and md. The tabs command now supports multiple browser profile directories and can emit JSON with things like group and pinned state. That's huge for desktop automation.
Alex: He also integrated that output into a tiny rofi script used for keyboard-driven tab switching. Instead of relying only on browser CDP that missed sleeping tabs, the rofi script now uses the "edge" output and still activates tabs via CDP — so you get titles for sleeping tabs and a robust activation path.
Maya: On the MCP server side — that's Anand's lightweight tool server for agents — he added a read tool that returns file contents and metadata, including safe handling for binaries and base64 encoding. Practical effect: agents can fetch images, PDFs, or audio reliably without the ad-hoc size truncation that used to break workflows.
Alex: He also improved browsing-history scraping to handle a new Edge profile layout, deduplicate rows, and migrated tests accordingly. Plus some real engineering: Playwright and Chromium setup in the dev container was hardened so headless browser checks are reliable in Docker.
Maya: Non-obvious takeaway: when you automate lots of small desktop / browser integrations, consolidating responsibilities into a small well-tested CLI (one command with subcommands) prevents future friction — and makes the UI glue much easier to maintain.

Alex: Listener tip from me: if you use small CLI utilities to integrate with GUIs, add a JSON output mode and a tiny test that reads that JSON. Maya, how would you apply that?
Maya: I'd wrap the helper in a unit test that simulates a few profiles and checks the JSON fields I depend on — that way any browser or profile change breaks CI and is easy to fix.

Alex: Onto the tab manager extension Anand maintains — big milestone: a durable, Manifest V3 side-panel tab manager landed, plus a user guide and automated release workflow. What changed for end users?
Maya: Now there’s an end-user README explaining install and usage, a release GitHub Actions workflow that packages the unpacked extension and creates releases, and a set of UI improvements: status icons for Active/Closed/Unmatched records, throttled "mark as viewed" behavior so accidental focus changes don't pollute view metrics, and more robust reconciliation logic after browser restarts.
Alex: He simplified the matching rules so the extension prefers exact URL+title matches, or partial matches that preserve the same neighbors. That reduces dangerous auto-attaches — you get "Unmatched" records to resolve manually rather than wrong automatic attachments.
Maya: The net effect is a more trustworthy workflow when you close and restore tabs or when the browser restarts: records survive, you can add notes, mark things to keep, and restore context without guessing.
Alex: He also added tests covering reconciliation, UI DOM, and the background logic — so these behaviors are covered and less likely to regress.
Maya: Non-obvious takeaway: for extension tooling that tries to infer user intent (like matching closed tabs back to records), prefer conservative behavior. Automatic fixes are tempting but can introduce data errors; making the ambiguous case visible is safer.

Alex: My listener tip here: if you build an extension that matches ephemeral browser state, prefer visible manual reconciliation for ambiguous cases. Maya — how would you use that?
Maya: I’d expose an "Unmatched" queue with quick attach actions and a helpful heuristic preview (neighbors, timestamps) so reconciling takes two clicks, not a deep hunt.

Alex: Anand's research work also shipped some interesting projects: a public dataset of model outputs for different writing styles and an iterative ideation-protocol optimization project.
Maya: The writing-style project collected model outputs across tasks and styles and added a tiny web UI to browse examples. Practically, this helps pick a prompt style and compare models' voices side-by-side — very useful if you care about tone and craft.
Alex: And the ideation-protocol work is an explicit, iterative experiment: design a prompt, evaluate it with a rubric, iterate the prompt, and measure which version produces better ideation. That’s a good pattern for anyone trying to design prompts with repeatable outcomes.
Maya: Non-obvious takeaway: treat prompt design like any other engineering problem — define a rubric, automate comparisons, and iterate. You’ll get more reliable gains than hand-tweaking ad hoc.

Alex: Quick tip from me: when you're trying new prompt styles, save a small reproducible test harness — a few tasks, a fixed rubric, and scriptable model runs — then iterate. Maya, do you have an example of how you'd apply that?
Maya: I’d pick three representative prompts for a recurring task (e.g., meeting notes), run them across two models, collect answers, and score with five binary questions. Then pick the best and keep it in a versioned SKILL file.

Alex: Anand also updated his podcast generator tool for a group chat: the script now includes the previous two weeks' podcast scripts as context when generating a new week's script, but explicitly tells the model not to repeat or recap — only to use the past for continuity.
Maya: That's a thoughtful design detail — context helps continuity without rehashing. The tests verify the prompt includes "PREVIOUS EPISODES — CONTEXT ONLY" and that only the last two episodes get pulled in, so the model sees recent context without being overloaded.
Alex: Practical effect: the weekly script will feel coherent across episodes while avoiding redundancy. Nice for serialized shows or newsletters.

Maya: Tools fixes too — a web-scraper used for capturing ChatGPT conversations was updated to correctly capture little writing blocks the model or user could edit in-place. That means saved transcripts are richer and less likely to drop important author-supplied notes.
Alex: And the LLM pricing project had a small but important scraper fix: model names in the leaderboard sometimes have extra lines; Anand changed the extraction to take only the first line. That kept the TSV extractor stable and avoided malformed exports.
Maya: Small parsing fixes like that prevent downstream analytics from failing silently. Non-obvious takeaway: scrapers are brittle; include defensive parsing and document assumptions so future runs are easier to diagnose.

Alex: Final listener tip from me: when you scrape a third-party leaderboard or table, extract the canonical part of a cell (first visual line or a machine name) and record both the raw and parsed versions. Maya — how would you apply that?
Maya: I’d store the raw HTML text and a normalized field side-by-side. If parsing changes upstream, you can re-run normalization without re-scraping.

Maya: Two quick extra practical ideas to leave listeners with. One: keep your agent skills organized in two places — codified, machine-friendly files for coding agents, and slightly richer, human-explanatory SKILL pages for broader use. That’s exactly what Anand did by moving or archiving some skills and clarifying where things live.
Alex: And two: instrument your small utilities with JSON modes and tests. We saw that everywhere in these changes — from the edge CLI to the MCP read tool — and it pays off in robustness.

Maya: That's a wrap for today. Thanks for listening to Anand's Weekly Codecast.
Alex: Catch us next week for more of Anand's tool-building, writing, and experiments. Bye for now!
Maya: Bye!