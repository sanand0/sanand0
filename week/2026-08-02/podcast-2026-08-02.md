Alex: Hello and welcome to Anand's Weekly Codecast for the week of 02 August 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week in Anand's personal scripts and tooling—this is the repo with all his dotfiles, shell helpers, and small utilities. What landed that matters most?
Maya: He added a couple of small but very practical pieces. The transcription tooling now writes the transcription model and dollar cost into the note's YAML front matter. That means every saved transcript carries which model produced it and how much it cost.
Alex: Why is that handy for everyday work?
Maya: Two big reasons. First, traceability—if you later wonder which model produced an answer, the info is right in the file. Second, cost accounting: you can roll up small costs from many transcriptions to understand usage and budgeting. He also updated tests to assert the metadata is present, so it's resilient.
Alex: Anything else in that scripts world?
Maya: Yep—email/HTML tooling was improved so a single utility can accept either Markdown or pre-rendered HTML and still generate a premailer-friendly email for Gmail. Practically, you can now design an email in HTML, pass it straight through, and still get the inlining and sanitization the tool does. Saves a convert-then-send step.
Alex: Nice. Any reliability or UX tweaks?
Maya: Lots. The MCP server got a middleware that ignores unknown parameters instead of failing—handy because different agent frontends sometimes add extra fields like description. He also made the server build a dynamic docstring showing mounted paths and whether they're read-only, plus added a safer download/read behavior so tools always return embedded resources. Those are defensive changes that make automation more robust when LLMs call local tools.
Alex: Small utilities too?
Maya: He added a fish-usage script that scans your Fish shell history and shows which custom functions you actually invoke—great to clean up cruft. And rofi helpers now log which clipboard transforms or prompts were activated so Anand can see what he actually uses. There are also some text-normalization fixes (em-dash handling) and an archive of old fish functions. Overall, it's maintenance that pays off daily.

Alex: Moving to another cluster: Anand's new LinkedIn CLI landed inside the scripts collection—it's a full tool that fetches profiles via a CDP/browser session. Tell me about that.
Maya: It's a big one—playwright-based, cache-aware, tests included. The goal is to capture a profile snapshot from a logged-in desktop browser session without needing extra auth steps. Practical effect: if you have Chrome running with your session, the tool can pull a clean structured export for research or archiving. Anand also added tests to avoid fragile expectations like needing an open tab first.
Alex: That’s the sort of brittle thing you want well-tested. Good move.

Maya: Next big area—Liveform, his workshop/live-survey server. This one got a thoughtful, user-facing upgrade: question randomization, audience segments, nicer results UI and a pinned-QR feature.
Alex: Explain randomization and segments in everyday terms.
Maya: Randomization lets Anand mark a single-choice or multi-choice question as “randomize: true” so each respondent sees the choices in a stable but per-email randomized order—useful to avoid order bias while keeping results readable. Segments let you create sets of alternative questions and show exactly one variant per respondent, deterministically based on their normalized email. The result is better experimentation and fairer comparisons without leaking PII—the selection is a hash, not raw email.
Alex: And the QR pin?
Maya: Small UX win: clicking the QR now pins it to the top-right fixed position so phones can scan even when you scroll—handy during live workshops. He also tightened accessibility and counts (respondent vs. responses) and added server-side checks so only assigned questions are accepted back. These make running a live session much smoother and safer.

Alex: Browser tooling got some love too—the tab manager extension saw a release.
Maya: Right. The tab manager now recovers much better from browser crashes and restarts. It drops provisional snapshots that were never fully created, recovers closed-but-should-be-retained records, and adds a setting to choose whether the toolbar button opens the side panel or a full tab (defaulting to a tab). The reconciliation logic was careful—matching tabs to stored records only when there’s enough evidence, and leaving ambiguous cases unmatched for manual resolution.
Alex: That reduces accidental mis-attachments and lost notes—super useful if you keep many tabs.

Maya: On the LLM side, Anand updated his LLM pricing site. He added an “intelligence” view with an intelligence v. cost curve, a new HTML page with interactive labels, and tooling to capture screenshot sequences so he can make model-evolution videos.
Alex: So you can see how cost and capability changed over time and make short animations?
Maya: Exactly. He added a CLI that drives a headless browser, snapshots the chart for each model and date, and a small script to stitch those images into webm videos. He also updated the model pricing table with newer models and prices—helpful for anyone tracking cost-capability frontiers.
Alex: That’s a neat way to communicate trends visually.

Alex: Anand's research repo also had an interesting thread: simplification prompts.
Maya: Yes—he ran an experiment asking models to “answer simply” (like ASD-STE100) and measured whether that instruction reduced the models' deeper thinking quality. The write-up and evals show that forcing too-simple style can shrink sources, shorten reasoning, and sometimes worsen correctness—an important reminder that style constraints can change the model’s internal process, not just the presentation.
Alex: Non-obvious takeaway: instructing “make it simple” is not just formatting, it can change the model’s search strategy and evidence usage. Good cautionary note for prompt engineering.

Maya: A few smaller but practical updates across projects: the AI Pipe backend had pricing updates for newer model families, so the hosted token service reflects realistic provider pricing; Anand created a tiny MCP worker repo that packages his SKILL.md files into a Cloudflare Worker so he can expose selected skills as MCP tools; and he added Claude desktop to his Linux setup notes and swapped in Monaspace font—personal but useful for reproducible dev environments.
Alex: Both the worker and pricing changes show a mix of publishing and operational hygiene—keeping the external interfaces useful and the developer environment consistent.

Maya: And the blog? He published posts—one about “Agent experience is the new user experience” and another on model cost-capability strategy—plus a bunch of prompt and prompt-system updates. Lots of content and prompt refinements went live that support the tooling updates we just discussed.
Alex: So the ecosystem: tooling to run agents and collect data, website to visualize model economics, and research to evaluate agent behavior. It all ties together.

Maya: Non-obvious takeaways across the week: small metadata wins scale. Putting model and cost into transcripts, logging which prompt you used, or recording which transform you clicked gives you data to improve workflows later. Also, deterministic selection (hash-based segments/randomization) buys you clean A/B-like splits without extra tracking.
Alex: And from research: changing surface constraints (like “write simply”) can change the internal process. That’s an important guardrail when you optimize for readability.

Maya: Listener tip time. My quick tip: if you run any note-taking or transcription system, add model and cost metadata to the file frontmatter now. It’s a 10–20 line change but immediately helps debugging, reproducibility, and billing. Alex, how would you apply that?
Alex: I’d add model+cost to my meeting notes pipeline and then run a weekly script to summarize cost per project. It would let me see where transcription spending concentrates and whether a cheaper model yields similar accuracy for certain meetings. Good tip.

Alex: My tip: when you run live polls or workshop forms, use deterministic segmentation and per-email randomized choice orders instead of one-shot randomization. It gives stable splits for analysis and keeps results interpretable. Maya, how would you use that?
Maya: I’d use segments to test two different wording variants across the audience and randomize choice order for preference questions so order bias is eliminated. Then I’d compare the segment slices with the same analytics pipeline—cleaner signal, less noise.

Maya: That’s it for this week's round-up. Anything else to add?
Alex: Just a reminder: small instrumentation investments pay off. Anand’s week shows that—metadata, logs, and deterministic little rules make big downstream differences.
Maya: Agreed. Thanks for listening.

Alex: Goodbye for the week!
Maya: See you next week on Anand's Weekly Codecast.