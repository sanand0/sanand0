Alex: Hello and welcome to Anand's Weekly Codecast for the week of 16 August 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week. Let's start with the toolkit that runs Anand's laptop and servers — the setup scripts, dotfiles, and productivity utilities he keeps in one place.
Maya: Right. Anand did a lot there: modernizing model defaults, rewrote an old shell utility in Python, archived unused bits, tightened tests, and shipped a new scheduler. Can you unpack the biggest practical changes for people who just use these machines?

Alex: Sure. The visible part is a gentle migration to newer AI models — he bumped defaults and pricing for a newer Gemini variant and also added GPT-5.6 Luna as a listed option. Practically that means the helper scripts that estimate cost or pick a model will now favor the newer, cheaper-latency options. So if you run a script that auto-selects a model, it will choose the fresher defaults and produce different cost estimates.
Maya: That sounds subtle but important. Changing the default model affects both cost and output style across tools — summaries, transcriptions, and anything using the local LLM CLI.
Alex: Exactly. He also replaced a long, brittle Bash picker used for pasting prompt fragments with a focused Python tool. The old rofi shell script was huge and hard to test; the new "prompt" CLI is shorter, has unit tests, uses a nicer log file, and integrates with desktop bindings. Practical effect: faster, tested prompt picking, and fewer weird clipboard oddities.
Maya: That’s a great modernization pattern — rewrite small but add tests and a cleaner interface. He also archived unused functions and moved one Vite/pages helper out into a small standalone script, so setup files are less noisy. Any other big infra moves?

Alex: A bunch. He added a run-at tool — it's a tiny scheduler that creates per-job systemd timers and marks completion. It now even sends desktop notifications when jobs finish. That’s handy for one-off scheduled commands without wrestling systemd units by hand.
Maya: Nice. And on the scraping and backup side, he tightened LinkedIn and WhatsApp backup scripts — more robust CDP usage, better DOM handling, cleaner error paths, and new unit tests. So those scrapers should be less fragile when pages change.
Alex: He also implemented cookie extraction over Edge's DevTools Protocol, so other scripts can fetch browser cookies from CDP instead of reading SQLite cookie files — less risky, more aligned with what browsers expose.
Maya: One practical idea: when you maintain local tooling that talks to browsers, prefer published CDP endpoints rather than on-disk data — it's explicit, avoids file-format drift, and can be filtered by domain.

Alex: Oh — he added a tested "prompt" command, wired the media-keys launcher to it, and expanded the unified test runner. The test runner now uses isolated Python environments with a "just test" entry that enumerates focused test recipes. That makes CI and local runs more reproducible without a monolithic lockfile.
Maya: Non-obvious takeaway: small, testable command-line programs plus a clear test matrix reduce the cognitive load when messing with dozens of helper scripts. If you want to keep a personal toolkit healthy, write small tests for user-facing actions and a predictable way to run them.

Alex: Moving on: Anand's blog got steady content this week. He published several short posts — a playful piece about whether to fix "poor" design in teacups, reflections on learning to dance, and a public experiment inviting readers to email his AI agent.
Maya: He also updated his internal "objectives" skill — the file that steers how answers should be formed for him — with versioning metadata and an expiry. Why is that useful?

Alex: Because his guidance is experimental. Adding a version and an expiry date forces a review cadence: prompts and steering rules evolve fast with his work, so making them time-boxed prevents stale assumptions from silently influencing output. Practical effect: anybody or any agent using that SKILL will notice when it’s time to re-validate.
Maya: I like that. One practical idea: when you rely on a persona or style guide, add a "stale after" date in the file and audit it quarterly. It saves accidental drift.

Alex: Next: talks and classroom materials. Anand published full pages for a guest class and an AMA — transcripts, chat logs, audio, and single-page HTML writeups with prompts to generate beautiful narrative pages. He’s also embedded prompts and a design for how the pages should look.
Maya: So he’s not just uploading slides; he’s turning events into reusable web narratives with audio and searchable transcripts. Why does that matter?

Alex: It makes ephemeral teaching materials durable. Students can re-run or re-prompt the narrative generator to create different versions (e.g., shortened summary, slide deck, assignment). Non-obvious takeaway: treat presentations as content pipelines — transcript → narrative → visuals → distribution — so a single session produces many assets.
Maya: Practical idea: if you teach, capture the transcript and the chat. Later you can ask an LLM to extract “testable claims,” “follow-ups,” and a short reading list — instant syllabus material.

Alex: On the data-visualization front, Anand added two interactive stories. One is an embassy-vs-GDP scatter exploring over- and under-representation of diplomatic missions; the other is a deep dossier of Glassdoor reviews for a company, built from scraped pages and first-party JSON.
Maya: That’s a lot: scraping, API capture, merging, and an explorable front end. What stands out to you?

Alex: Two things. First, the extraction tooling: for Glassdoor he built capture + fetch + extraction steps and tests to ensure counts and merges are correct. That means the visualization is backed by auditable data and tests that catch drift. Second, the embassy piece adds dynamic, searchable outlier labeling — you can filter and the outliers are recomputed for the current view.
Maya: Non-obvious idea: when you publish interactive data stories, ship both the visualization and the extraction tests. That builds trust and makes it easy to refresh the story later.

Alex: In the small web apps area, a lot of scraper utilities were improved to export Markdown and JSON — Twitter/X threads, Discourse topics, WhatsApp chats, LinkedIn invites. The scrapers now mount a tiny UI with "copy as Markdown" and "copy as JSON" buttons, plus tests to verify the exported shape.
Maya: That’s a great productivity win. Instead of manually copy-pasting messy HTML, you get readable Markdown that you can drop into notes, drafts, or a blog. Practical idea: if you frequently cite chats or threads, automate an export into a tidy Markdown template you trust.

Alex: On the movie explorer side, the site now shows the year in the tooltip and recomputes "outliers" dynamically when filters change — so if you filter to one genre or decade, outliers adjust to what's visible rather than being global. That makes exploration more meaningful.
Maya: Small UX change, big payoff: outliers should be relative to the current slice of data, not the entire dataset. Otherwise you highlight irrelevant things.

Alex: On model pricing and leaderboards he updated the model stats and fixed ELO rows — basically upkeep for the LLM cost/quality chart.
Maya: Those charts are only useful if the model mapping and scores are current, so that maintenance matters more than it looks.

Alex: He also pushed the weekly group podcast script — a helpful example of turning group chat into a two-host narration — and updated the small "push" recipe so podcast files can be committed and pushed easily. It's meta but neat: he uses tools to make the newsletter/podcast loop friction-free.
Maya: And his weekly notes (TIL) got updated with learnings about agents, verification, permissions, and breathing techniques — human things alongside the code.

Alex: Across everything there’s an explicit theme: make tooling robust, testable, and resumable. Replace ad hoc shell glue with small, well-tested programs; prefer explicit CDP and API usage over brittle file scraping; and make outputs (Markdown, JSON) ready for reuse.
Maya: And when you change defaults — e.g. model choices — do it with tests and visible pricing so you actually see the cost and behavior changes.

Alex: Okay — time for listener tips. Quick, short, actionable. My tip: add a timestamped expiry or "last-verified" field to any prompt, SKILL, or policy your agents use. It forces small periodic reviews and prevents silent drift. Maya, how would you apply that?

Maya: I would start by adding a metadata header to two of my commonly used prompt fragments, set a 3‑month expiry, and schedule a run-at job to remind me when it’s due. That way the reminder lands as a real task, not a vague thought. My tip: when you capture transcripts or chats, export them as tidy Markdown immediately — use a small script or the browser scraper that produces a one-file Markdown with metadata. Alex, how would you apply that?

Alex: I’d wire that export step into my meeting template: one button to record, one click to export Markdown to my notes folder, and then a short "summarize in three bullets" prompt. That way the costly stuff gets turned into reusable assets right away.

Maya: Love it. Any last quick thought?

Alex: If you run lots of tiny utilities on your laptop, build a short test that exercises the hot path — even a single scenario — and run it weekly. It catches regressions early.

Maya: Agreed. And remind yourself: short, tested tools beat clever long scripts every time.

Alex: That's all for this week. Thanks for listening!
Maya: See you next week — happy hacking and take care!