Alex: Hello and welcome to Anand's Weekly Codecast for the week of 23 August 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week — Anand was elbow-deep in tooling. Let's start with his personal scripts repo — the one that runs the little utilities he uses daily. Maya, what stood out to you?
Maya: So many small surgical changes that together make things cleaner and more reliable. A few examples: he upgraded his shell-usage reporter so it counts not only fish functions but also the scripts in ~/code/scripts; he simplified and hardened a call-transcription tool; he rewired the blog summarizer to use a different LLM configuration; and he tightened the agent and image-generation skills so the system behaves more predictably.
Alex: Nice summary. Walk me through the usage reporter change first — what actually changed and why it matters?
Maya: The usage tool used to only report fish function calls. Anand updated it to also detect invocations of his scripts directory, and to show three columns: uses, last-used timestamp, and whether the item is a function or a script. The practical effect is that he can now see at a glance which helpers he actually runs from the shell, and how often — which is great for pruning and prioritizing maintenance.
Alex: That’s useful. He added tests too, right?
Maya: Yes — tests that simulate history entries and verify the tool respects the time window, reports last use correctly, and distinguishes functions from scripts. Small addition, big payoff: tests keep the metric honest.

Alex: Next — the summarizer that updates blog metadata. I saw he switched the model pricing and behavior for blog processing. Can you unpack that?
Maya: Sure. Anand moved the blog summarization flow to a model configuration and cost assumptions better aligned with "luna"-style models — lower per-token pricing in his internal table — and changed the tool to default to more parallel workers for blog posts. He also made the summarizer call the model in separate API calls for description vs tags, so each task is explicit and measurable.
Alex: Why separate the calls?
Maya: Two reasons. One: it gives clearer cost and call counts — useful when you run many posts. Two: different tasks (short human-facing description versus a list of canonical tags) often need different prompting and maybe different tokens, so separating them reduces accidental token bloat and makes retries safer. Tests were added to lock in that behavior.

Alex: He also reworked transcribe_calls, right? I loved that commit message — it seemed like a simplification spree.
Maya: Exactly. The transcribe tool had a lot of complicated globbing and "scan pending" logic that rarely matched Anand's actual usage. He replaced that with a simpler interface: you pass an AUDIO argument (either an explicit path or a substring to match a file in ~/Documents/calls), and the tool finds the most likely match. He reintroduced a fast --list-changes mode, added --force to re-transcribe, merged two patch flags into one that auto-detects invalid sections, and made chunk retries automatic for malformed chunks. He flattened the cache layout to match other tools, and added tests for all these behaviors.
Alex: So the user interface is friendlier and the common paths are faster.
Maya: Yep. Simpler UX + targeted tests = less accidental complexity.

Alex: There were also a string of updates to the "skills" — the small instructions and safety rules Anand packages for agent helpers. Anything notable?
Maya: A lot. The system-level "skill-creator" docs got much more prescriptive: templates, forward-testing guidance, when to run a real-world test, and advice to avoid leaking context into agent runs. The image-generation skill got practical rules around transparency: use the built-in image tool first, prefer chroma-key + local removal, and only fall back to CLI model-native transparent output after explicit confirmation. He added a small local helper for removing chroma-keyed backgrounds. That reduces surprise behavior and makes image workflows safer and auditable.
Alex: In other words, avoid silent downgrades between tools and make fallbacks explicit.
Maya: Exactly — transparency both in images and in decisions.

Alex: He touched a server for agent tooling too — mcpserver — what changed there?
Maya: Anand made the tool outputs more machine-friendly. The bash tool now returns a structured result that includes status, an explicit ok boolean, and the trimmed output plus a saved full-output path when the output is large. The server writes big outputs to temp files and returns a pointer. That makes it easier for other agents or scripts to decide whether a result is a hard failure, a timeout, or just truncated output they should download.
Alex: Handy when an agent tries to parse CLI output and needs a stable schema.

Maya: Two other smaller but very practical updates: rofi-clip’s strip-tracking transform was improved to find and clean multiple URLs embedded in a chunk of text, preserving surrounding whitespace and notes; and a handful of browser/Edge tools got a new contents command that can optionally wake sleeping tabs and extract human-readable Markdown per-tab, while isolating extraction errors (so one broken tab doesn't kill the whole run).
Alex: Little improvements everywhere. Anything else in that repo we should flag?
Maya: He updated uv environments to recommend Python 3.14 in his setup instructions, and added many tests and examples — e.g., a with_server helper for local webapp testing. The pattern is clear: favor simpler UX, explicit fallbacks, and better observability through structured outputs and tests.

Alex: Great. Let's move to Anand's blog and website repo. There were a lot of content and build changes.
Maya: Yes. Two broad themes: content and build metadata. Anand rewrote descriptions and tags across many 2026 posts using the new summarizer, added new posts (things like “Local agents are good but slow” and “What to do less because of AI”), and fixed a few pages and small typos. On the build side, he changed how "skills" pages are produced: SKILL.md and README.md are merged into an _index-style output, fixed the export of corpus records used for embeddings, and added a simple "just serve" recipe to serve the generated public/ directory locally.
Alex: Why does the SKILL.md handling matter?
Maya: The site generates static pages from a mixture of pages and skill definitions. Previously some skill pages generated awkward URLs and duplicate entries. The update makes the site expose the skill directory (the README + SKILL) as a single canonical page, with correct sourcePath and description in the export. That matters for discoverability (search, embeddings) and for downstream tools that index the corpus.
Alex: And the summarize changes manifested in content edits?
Maya: Yes — you can see it in the refined descriptions and tag lists across many posts. The summarizer’s new defaults and prompt adjustments produced shorter, clearer descriptions and more consistent tags — better for readers and for agents that index the site.

Alex: Next up: the live classroom forms tool — Anand made a concentrated fix there.
Maya: Correct. He improved the results map and filter UI and made projection caching robust. Practically, the results page now groups segment controls into an accessible "segment group" control (so you can toggle which questions in a segment to include), the scatterplot supports brushing (drag to select points) with accessible keyboard toggles, and tooltips behave better when nodes have long text. On the backend, there’s a compact projection cache file (a small JSON map) with versioning and eviction so malformed cache files or very large maps won't break the UI; tests cover cache invalidation and malformed data handling.
Alex: Explain "brushing" for listeners who haven't heard the term.
Maya: Brushing is clicking and dragging across a visualization to select a set of points — like drawing a box around items in a scatterplot. Anand hooked that up so selection logic toggles correctly, the selection is keyboard-accessible, and omitted long outputs are preserved to a file if the UI trims them.
Alex: Those UX and robustness details really matter for workshop use.

Maya: Tests and accessibility bits were added — aria roles, role=group for the segment area, and a tooltip with role=tooltip. So it's friendlier for screen readers and better behaved in practice.

Alex: Finally, a small one: the LLM pricing repo.
Maya: He updated the model list and refreshed the ELO/cost CSVs and bumped the “last updated” date. That simply keeps the cost-vs-quality scatter updated — useful if you use it to pick models or track the frontier over time.

Alex: Before we sign off, let's give listeners a couple of quick, actionable tips inspired by this work. Maya, you go first.
Maya: Tip: When you run bulk AI tasks (summaries, tags, transcripts), set content-specific defaults — like a sensible "workers" value per content type and separate model calls for different outputs (short description vs tag list). It reduces accidental cost blowouts and gives you per-task observability. Alex, how would you apply that?
Alex: I’d add a simple wrapper around my blog-processing script: a small config mapping content type to worker count and to a pipeline of discrete steps (description, tags, keywords). Then I’d run a dry-run mode that reports estimated tokens/cost before execution.

Alex: My tip: keep simple telemetry about your own workflows. Anand's new fish_usage that counts script usage and shows last-used timestamps made it obvious what to delete or improve. Add a tiny usage reporter for your shell aliases and scripts so you can prune cruft. Maya, how would you use that?
Maya: I’d use it to find rarely used commands and convert them into one-liners or archive them. For team-shared scripts, the report helps prioritize documentation and tests for the most-used ones.

Alex: Lovely. Any closing notes?
Maya: This week felt like clean-up + guardrails: better defaults, simpler interfaces, explicit fallbacks, and lots of tests. Small changes but they compound into more reliable workflows.
Alex: Agreed. Thanks for listening. We'll be back next week with more of Anand's work.
Maya: Bye for now — happy hacking and see you next week!