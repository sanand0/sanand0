Alex: Hello and welcome to Anand's Weekly Codecast for the week of 12 July 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week. Shall we start with the thing that touched almost everything — Anand's blog and site tooling?
Maya: Yes. He did a lot there: better search, a canonical corpus export for agents, more careful tag workflows, offline related-posts, embedding fixes, and a bunch of new posts. What's the short version?
Alex: He turned the blog into something that’s easier for both humans and automated agents to read, cite, and reuse. There’s now a deterministic JSONL corpus export — one record per post — plus a small agent-facing bundle (tags.json and llms.txt) so an agent can discover the archive without crawling the HTML.
Maya: Why does that matter? I mean, people already scrape sites.
Alex: Two reasons. First, the export is canonical and compact — it’s a stable API-like snapshot you can commit to Git. Second, agents love compact, predictable inputs. Instead of guessing where a piece of content lives or which metadata to trust, an agent can start from the corpus file, know the canonical URL, title, date, and tags, and not waste tokens hunting the site layout.
Maya: He also reworked related posts, right?
Alex: Yes. Instead of building related-posts at build time from heavyweight embeddings, he moved the ranking offline and made the output compact — slugs only. That makes the site build deterministic and the related-data easy for other scripts or agents to consume. He also added code to invalidate old embedding vectors by introducing a cache hash version, so stale vectors don’t quietly lie to you.
Maya: That’s subtle but important. If an embedding contract changes and you don’t mark it, you get wrong neighbors and surprising search results.
Alex: Exactly. And he added tests around all this — so the corpus export, the related-posts JSON, and the tag workflows have acceptance checks. He also changed the upkeep flow to a single, documented command you can run locally — so the whole pipeline is reproducible.
Maya: What about tags? I noticed a lot of tag work.
Alex: He built a reviewable tag vocabulary generator that looks at existing keywords, proposes canonical slugs, and writes a reviewable file. There’s a tiny ledger to collect proposed tags from the summarize run, and a new script to promote a proposed tag after human review. So metadata moves from free-form keywords to a maintained taxonomy.
Maya: Practical idea: if you run a blog with a long history, don’t mass-change tags automatically — collect proposals for review. That prevents accidental vocabulary drift.
Alex: And a few other practical wins: Pagefind-based site search was added as a single-page integration (so only the search page loads the heavy JS), media-embedding guidance for audio/video/iframes was documented in the README, and the "summarize" upkeep now requests only the missing fields from the model instead of regenerating everything.
Maya: Non-obvious takeaway: treat agent exports as first-class citizens. Adding llms.txt and tags.json means you can hand the archive to an agent and say "start here" — much cheaper and clearer than teaching the agent how to scrape.
Alex: Totally. One quick action you can try: run the new update flow locally (the just update / setup.sh aliases) and inspect public/blog/corpus.jsonl and public/llms.txt — you'll see how an agent could bootstrap without scraping.

Maya: Next up — Anand's talks and workshop materials. He added full workshop pages, transcripts, prompts, and a reusable "talk-story" skill and templates. What's new and why it's useful?
Alex: He published detailed artifacts for several recent events: a full workshop called "When Data is for Agents", a VizChitra dialogue, an IIMB Multivariate Data Analysis session, and a school-teachers session. For the big workshop he committed transcripts, the chat logs used in the session, benchmark prompts, a cataloging script, and a small skill that codifies "how to make a corpus agent-ready".
Maya: That’s a lot of provenance. Why do folks care about that level of source material?
Alex: Because it turns ephemeral workshop experiments into reproducible research. Anyone can replay the prompts, see the original model interactions, re-run the benchmarks, and audit claims. He also improved the "talk-story" skill — it now includes a chunked-generation workflow, a QA pass that checks quote attributions and visual contrast, and a prompt template for future stories.
Maya: Non-obvious thing: transcripts often misattribute quotes — he added explicit checks for that in the skill.
Alex: Yes — he made quote attribution checks a first-class part of the generation QA. The practical result is fewer embarrassing misquotes on published pages and more trustworthy stories. If you give talks and publish write-ups, adopting a similar chunked generation + QA pass will save you corrections later.
Maya: Idea to try: when you publish a talk page, include the raw share links for the chat sessions and a tiny manifest so readers can inspect the raw evidence. That’s what Anand did — it makes claims verifiable.

Alex: Moving on to the scripts and tooling repo — this was huge. He added new small apps and major changes: emailai for building reply prompts, musictag for MP3 tag management, meetcaptions for Google Meet and Teams caption capture, edge_tabs to snapshot browser tabs, observability for MCP server runs, and refinements to summarize.py to persist tag proposals and request only missing metadata.
Maya: That’s the "operational" work that keeps everything smooth. What’s the observable effect?
Alex: Better automation and safer interactions with agents. For example, summarize.py now uses the canonical tag vocabulary when suggesting tags, caches the vocabulary, and writes proposed tags to a ledger. That means less manual cleanup and a reviewable pipeline for new tags. The observability changes to the local MCP server add structured JSON logs with bounds and redaction so you get actionable telemetry without leaking secrets. The meetcaptions and Teams support create local Markdown transcripts from live captions, helpful for notes and accessible archives.
Maya: And the new musictag tool — what's that doing here?
Alex: It’s a pragmatic utility: manage ID3 frames from a CSV, keep certain personal frames (ratings) preserved, and clean spammy frames. It's a good example of a tested, idempotent CLI that an agent could drive safely.
Maya: Non-obvious takeaway: when you build agent-facing scripts, favour small, idempotent commands, explicit state, and clear dry-run modes. Anand added tests for all of these scripts — that matters.
Alex: Exactly. Also, the summarize flow now only asks the model to generate missing fields rather than regenerating everything — that saves tokens and reduces churn. He added tests that assert the prompt only requests the specific fields requested, which is an excellent pattern for cost-control.

Maya: He also touched the small web tools collection —captions, LinkedIn invite scraping, and a few housekeeping things.
Alex: Right. He unified captions for Meet and Teams into a single toolset, added robust fallbacks for obfuscated DOM selectors, added tests for Teams, and simplified the LinkedIn invite scraper to be less brittle. He also removed some linting steps from the build to speed iteration — a pragmatic choice for small static tools.
Maya: Practical idea: use the bookmarklet pattern he used for captions — it keeps everything local and avoids dependency on external services. And add polling plus a stability heuristic for captions, which he did.
Alex: One caveat: less linting speeds iteration but be careful if multiple contributors rely on consistent formatting.

Maya: On to data storytelling — the small interactive map story about a fourteen-minute walk that took an hour. What did he add?
Alex: He added a full scrollytelling map story: routes as KML, an index.html built for MapLibre, and design instructions to inline assets so the story runs on GitHub Pages. The story shows the detour between expected and actual routes and includes exact coordinates and calls for mobile-friendly pacing.
Maya: Nice concrete lesson: include the actual route geometry and keep map tiles external — keep the story self-contained aside from tile fetches. That's a resilient publishing pattern.

Alex: Finally, the LLM pricing project — he refreshed the data and the narrative to mid-2026. He updated the model scores and prices and rewrote the storytelling to emphasize a portfolio approach: cheap routing models, mid-tier everyday models, and expensive frontier models.
Maya: That's an important framing: models are now a toolkit, not a single hill to win. Practically, choose the cheapest model that does the job, and reserve the most costly ones for rare problems.

Maya: Okay, listener tips. I’ll go first. Tip: Inspect the blog’s public corpus export before you build any agent that scrapes the site. Use public/blog/corpus.jsonl as your primary index and llms.txt to seed agents. How would you apply this, Alex?
Alex: I’d point an experiment agent at llms.txt, load tags.json to decide which posts to prioritize, and only fetch the listed corpus slugs — that keeps token cost low and makes results reproducible. I’d also add a small local test harness that asserts the agent only requests the public URLs listed in corpus.jsonl.

Alex: My tip: when using embeddings, include a cache hash version and invalidate vectors when the embedding contract changes. Anand added CACHE_HASH_VERSION and reports when vectors created under old contracts are invalidated. How will you apply that, Maya?
Maya: I’ll add a versioned hash to our embedding pipeline and emit a clear console message if migrated vectors are found. That way, re-runs won’t silently reuse incompatible vectors and I won’t chase mysterious retrieval errors.

Maya: Any other quick tips from the week?
Alex: One more: if you maintain a long-lived blog, capture proposed tag suggestions in a review ledger rather than auto-editing sources. Human review avoids vocabulary rot. Would you add that to your content workflow?
Maya: Yes — I’ll adapt our tag-suggestion step to write proposals to metadata-tag-proposals.yml and require a one-line description before promoting.

Alex: That’s it from us. Thanks for listening — we hope you found a useful idea or two to steal from Anand’s week.
Maya: See you next week. Goodbye!
Alex: Goodbye!