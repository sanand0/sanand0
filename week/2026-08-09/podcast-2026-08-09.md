Alex: Hello and welcome to Anand's Weekly Codecast for the week of 09 August 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week on the blog — new posts, a pile of prompt and skill tweaks, and a faster LinkedIn↔blog mapper. Tell me what landed and why it matters.
Maya: Anand added several essays and experiments — a write-up about using AI to double-check tax returns, a hands-on report comparing letting agents run code versus calling CLIs, and a careful experiment showing that forcing a model to write in ultra-simple language harms its reasoning. Practically, that’s two things: more public stories and a set of workflow lessons you can copy.
Alex: Walk me through the tax one in plain language.
Maya: He let ChatGPT calculate his Indian tax independently from raw sources — AIS, bank statements, Form 26AS — then used the auditor as the final decision maker. The model found a discrepancy; the auditor verified and filed. The useful pattern is Expert + AI: let the model crunch raw data, then present precise evidence to the human expert and get the final sign-off.
Alex: And the MCP vs Shell post — that’s the code-as-mode idea?
Maya: Yes. He experimented with exposing his local CLI through a tiny MCP server so a chat agent can run his tools. The agent that could run code and loop did much more thorough work than the one constrained to narrow plugin calls. The takeaway: agents that can run code iterate like a developer — they can loop, re-fetch, re-check — so design your integrations to let them do safe, auditable loops.
Alex: I noticed the prompts and SKILL files changed too.
Maya: He tightened several “skills” — short, reusable instructions the site uses when it asks agents to act for Anand. The edits push toward making outputs asset-focused and reusable: prefer building something that can be reused automatically, include verification evidence, and pick where to store the result. That’s useful if you design agents that should produce deliverables, not just chat text.
Alex: And the LinkedIn-blog mapper?
Maya: Big refactor: it now prefers explicit overrides and front-matter links, strips HTML before scoring, and only does corpus-wide scoring for unresolved posts. That preserves earlier manual decisions, runs much faster, and sends a small review file for ambiguous matches. Practically: less time waiting, fewer false positives, and an auditable override path.
Alex: Non-obvious takeaway?
Maya: Treat manual editorial decisions as first-class: preserve them as overrides so automation only helps where you want it to, instead of silently undoing human judgment.

Alex: Onto Anand’s developer scripts — there were huge changes to his MCP server and a bunch of agent-facing tweaks. What’s new there?
Maya: The MCP server got a major refactor and feature lift: better structured logging, event JSONL for observability, explicit input/output schemas, safer save_file/download_file handling, and improved test coverage. He also ensured tool annotations are ChatGPT-compatible and added more robust compatibility for different clients.
Alex: Why does that matter for someone building agent integrations?
Maya: Two big things. First, clearer observability — every tool call is now logged with structured metadata so you can trace what the agent did and diagnose failures. Second, correctness and safety — limits for uploads, path traversal protection, explicit schemas and annotations help downstream callers validate arguments and avoid subtle security bugs when agents control local tools.
Alex: He also nudged devtools skill to try APIs before DOM scraping, right?
Maya: Yup — the skill now suggests inspecting fetch/XHR and replaying stable API calls before resorting to fragile DOM scraping. That’s a small change with outsized payoff: replayable API calls are deterministic and cheaper for repeated runs.
Alex: Any other notable changes in the scripts repo?
Maya: He added GPU-aware ffmpeg shortcuts, installed an agent CLI, and updated docs and migration guides about moving to the newer codex/gpt runtimes — all pragmatic developer ergonomics to make experiments faster and more repeatable.
Alex: Non-obvious takeaway?
Maya: When agents operate your environment, treat tools as first-class APIs: annotate them, log them, and keep outputs structured. That turns debugging from guesswork into inspection.

Alex: Liveform — the survey tool for workshops — got feature updates too. What landed there?
Maya: The two big user-facing features: respondents can now edit their answers (an editable flag per question) and the results UI got theming and brushing improvements. Under the hood, the server loads environment variables from .env on startup, and embedding settings were made smarter to prefer Gemini when present and warn cleanly if credentials are missing.
Alex: So for a teacher running a live workshop, what’s the practical effect?
Maya: Now participants can correct mistakes without creating multiple counts, and presenters can toggle light/dark themes on the results page and use brushing to select points on maps. For organizers, .env loading and clearer embedding warnings make local setup less error-prone and the UI more pleasant and accessible.
Alex: Any testing or safety angles?
Maya: Yes — they added tests for preflight checks, response write access, and ensured the map interactions and selection keyboard controls are robust. Also, embeddings only run when keys are present and the code warns otherwise — important to avoid silent failures in semantic maps.
Alex: Non-obvious takeaway?
Maya: Design surveys so the human is always in control: permit edits where sensible, count only the latest per respondent, and make UI affordances (themes, keyboard navigation) part of accessibility, not an afterthought.

Alex: Tools — there’s a major new app, LiveGrid, styling tweaks for slides, and fixes for the older WhatNext app. Summarize.
Maya: LiveGrid is a new, static, browser-only live multi-user prioritization grid that syncs via Cloud Firestore. It fits the whole grid to the viewport, supports import from tab-delimited rows, QR code sharing, offline editing, and simple deletion semantics. Slides got a nicer subtitle handling — subtitles now make it into the window title (cleaned), a modal closes on Escape, and body/table styling improved. WhatNext had drag fixes and some modernized DOM code.
Alex: For workshop facilitators, LiveGrid sounds useful.
Maya: It’s perfect for remote or in-room prioritization sessions: generate a room UUID, share via QR, invite everyone to move items, and import a spreadsheet of notes. But watch Firestore quotas — the README warns about write-volume and last-write-wins semantics.
Alex: Non-obvious takeaway?
Maya: When you build live collaborative tools, prioritize graceful offline behavior and a clear ownership model for deletes. Also, small UI touches — Escape to close, title normalization — drastically improve polish.

Alex: Talks and media — anything notable?
Maya: Anand added a full Data Hack Summit page: transcript, prompts, a comic panel concept and a recorded audio release. He also updated several other talk pages and embedded assets. So more reproducible public materials for his workshops and talks.
Alex: Why is publishing like this useful beyond sharing slides?
Maya: It preserves the exact narrative, prompts, and conversation context. That makes it easier for others to reproduce the demos, reuse the prompts, and teach from the same materials. It’s verification plus reuse.

Alex: A few smaller but neat items: the art styles project and pricing tool updates.
Maya: He re-ordered and expanded art styles, especially adding a big set of black-and-white and comic-flavored styles to explore unusual illustration aesthetics. That makes prompt-driven image generation richer. The LLM pricing tool got updated ELO and frontier data — the README timestamp changed — so the cost-vs-quality frontier graphs are fresher.
Alex: Practical idea there?
Maya: If you generate images, keep a curated style bank and re-order it by what you actually like. For model selection, keep an eye on the frontier chart so you pick models that aren’t dominated on cost and quality.

Alex: And finally, Things I Learned got refreshed.
Maya: Yes — new notes about efficient video compression (WEBM can be tiny), transcription costs across providers, package install tips, and toolchain notes. Little reminders but ones that save time and money in practice.

Alex: Listener tips time. Quick, actionable: one from you, one from me, and ask how the other would use it.
Maya: Tip: If you let a model audit or calculate something important, always feed it raw sources, not summaries, then ask your expert to confirm in writing. How would you apply that, Alex?
Alex: I’d run models to pre-fill a short checklist or draft, then send the checklist plus the model’s evidence to the human reviewer with a one-line decision question — and save that reviewer’s answer as the single source of truth. Your turn: quick tip?
Alex: Tip: When you expose local tools to agents, log every tool call with structured metadata (who, which tool, inputs, sizes, outcome) and keep an append-only event file. How would you apply that, Maya?
Maya: I’d start with a tiny audit endpoint that tailors logs into a single JSONL stream for quick postmortems. In a workshop, it’d be my go-to for tracing an agent’s steps when something unexpected happens.

Alex: That’s a wrap for this week. Thanks for joining us as we walked through Anand’s commits and what they mean.
Maya: We’ll be back next week with more updates, experiments, and practical takeaways from his projects.
Alex: Bye for now!
Maya: Bye!