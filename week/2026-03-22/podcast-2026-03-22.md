Alex: Hello and welcome to Anand's Weekly Codecast for the week of 22 March 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week — let's start with the thing that ate most of Anand's time: his talks site. He rebuilt and published a bunch of talk pages, full transcripts, sketchnotes, audio, videos, interactive critique pages, and even an ideas catalog for institutional audiences.
Maya: Right — the practical bit is that a lot of live presentations were turned into rich, linkable web artifacts: full transcripts that pop up in-page, embeddable critique iframes, downloadable audio, and slide/video integrations. Anand also produced structured "ideas" pages for different audiences like faculty, funding teams, and academic councils.
Alex: Why that matters: talks usually disappear as slides or a YouTube clip. Anand's approach makes the content searchable, reusable, and auditable — anyone can open the transcript, see the prompts that generated supplemental material, or follow the evidence trail behind an idea.
Maya: Non-obvious takeaway: he's treating talks like reproducible research — prompts, sketchnotes, generated images, prompts.md and follow-up playbooks all live next to each talk. That makes it easy to re-run the process, update examples, or embed follow-up exercises. Practical idea: if you give a talk, publish the transcript + an "action list" page (what to try next) so attendees can immediately act.

Alex: Next biggest: the education analysis work — real heavy lifting on student exam logs and TA support. Anand added a v2 analysis pipeline that ingests parquet exports, builds TA-ready outputs, and creates a "learning-support" pack: priority queues, ready-to-submit lists, hidden-test watchlists, thrashing reports.
Maya: That's the repo that turns raw event logs into things TAs can actually use. He added scripts to generate CSVs, an HTML playbook for TAs, and code to compute segments like "no traction", "thrashing", and "false confidence / hidden-test risk."
Alex: Why it matters: raw logs are useless unless transformed into prioritized, actionable tasks. This pipeline surfaces which learners need quick nudges, which units have misleading public tests, and where a simple prompt/test tweak would fix a large chunk of failures.
Maya: Non-obvious takeaway: he also focused on operational details — thresholds, reproducible SQL queries, DuckDB/parquet for resumable analysis, and concrete output formats the operations team can sweep into morning routines. Practical idea: if you run courses, start with a simple "ready to finish" rule and a "hidden-test watchlist" — it buys a lot of credibility with minimal work.

Alex: Closely related: the replay material and PyConf prep. He generated replay exemplars — short student replays that show different problem-solving styles — and scripts to render them for talks. That makes teaching moments concrete on stage.
Maya: Those replays are gold for instructors because you can show a clean correct approach, a productive struggle, and a thrashing example for the same question. Practical: use one short correct replay to build mental models, then the messy ones to teach debugging protocol.

Alex: On the blog side, Anand built an embeddings pipeline and a blog-map. He embedded posts with Gemini Embedding 2, cached models, ran PCA→UMAP→clustering, and produced an interactive blogmap + related-posts outputs.
Maya: In plain terms: every post got turned into a numeric fingerprint (an embedding), then projected into 2D for a map you can explore. He also exported related-post candidates, cluster summaries, and diagnostics.
Alex: Why it matters: this makes related-post suggestions, search, and the "what changed over time" story far more reliable. Non-obvious: he cached PCA/UMAP and model artifacts so coordinates are stable across runs — important for permalinks and reproducibility. Practical idea: if you add embeddings to a site, persist fitted transforms and only project new items so readers don't see a shifting map every time you re-run the pipeline.

Maya: Anand also added a new data-stories piece: three novel data visualizations packaged as an interactive slide deck with synthetic data and a full design appendix. He didn't just make charts — he documented the search, failures, design iterations, and why each visualization is novel.
Alex: That's useful for two reasons: first, it demonstrates a repeatable creative process for visualization; second, the three new charts solve specific analysis problems (where typical charts fail). Non-obvious takeaway: he validated novelty by searching first, so the "novel" claim is supported by process, not just assertion. Practical idea: when inventing a chart, produce a short appendix that shows the synthetic data and why standard choices fail.

Alex: Over in the demo tooling space, Anand expanded his "API agent" demo to include the FRED economic-data API. He added config prompts, a validator prompt, and a UI toggle that simulates a "cheap" model that fails first then recovers — perfect for teaching robustness.
Maya: Two neat bits there: the FRED integration gives live economic data in demos; the "cheap vs good model" toggle is a demo pattern that shows how an agent can recover from a first-pass error. Non-obvious takeaway: when you demo agents, intentionally include a recoverable failure so you can demonstrate validation and remediation. Practical idea: add a tiny "simulate failure" mode to demos to show end-to-end error handling.

Alex: He also improved the slide editor tool: independent log-scale title/subtitle sizing, a favicon, and tests to assert the sizing behavior. Small but meaningful quality-of-life work.
Maya: Yes — independent controls for title and subtitle sizes are a thoughtful UX lift, and automated tests mean those design controls won't regress. Practical idea: when you expose scale sliders, keep them logarithmic (10% steps) and test the rendering behavior programmatically.

Alex: A few housekeeping items: he updated TIL notes, switched CLI examples to a current backend, refreshed trending-repos snapshots, and added a Google Search Console verification tag to his site template so the site is verified.
Maya: Those are the little maintenance things that keep everything working. The trending repos update is useful if you follow his "things I learned" feed; switching to current backends avoids confusing examples.

Alex: Before we go, quick listener tips. I'll go first: if you run a course or workshop, build one automated artifact that the TA uses each morning — even a single CSV with "top 20 learners to check" — and wire that into a 10-minute routine. How would you apply that, Maya?
Maya: Love that. I'd take Anand's pattern and start with "ready_to_finish.csv" for one unit next week, run it once, and use it to prioritize hot fixes for 30 minutes. My tip: when you add embeddings to a site, persist the dimensionality-reduction and clustering models and only reproject new items — it keeps visualizations stable and shareable. Alex, how would you apply that?
Alex: I'll take it to my newsletter: generate a small, cached UMAP for posts and use it as a static interactive image we can link to — no surprise shifts when I add new posts.

Maya: One more quick tip from me: when you demo an LLM or agent, add a controlled "cheap model" mode that intentionally fails first then recovers on retry. It reveals your validation strategy and builds trust. Alex, how will you use that?
Alex: I'll add the same pattern to one of my client demos: show the agent catching and fixing an API parameter bug on the second attempt so stakeholders see the full verification loop.

Maya: That's it for this week. Thanks for joining our tour of Anand's week: rich talks, operational education analytics, site intelligence via embeddings, fresh visualizations, and thoughtful demos.
Alex: See you next week — keep shipping notes, stories, and experiments. Goodbye!
Maya: Bye everyone — happy building!