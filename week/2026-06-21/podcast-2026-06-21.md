Alex: Hello and welcome to Anand's Weekly Codecast for the week of 21 June 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Big week. Let's start with the classroom tooling Anand maintains — the little app that runs live, editable workshop forms from his laptop for students and event attendees.
Maya: Right — think of it as the workshop survey and live-question engine. Anand made a bunch of changes: added a ready-made form for the data-stack talk, improved how the browser polls the server, made login sessions last longer, and taught the server and client to show per-question response counts.

Alex: The practical effect is immediate. The new form let's him run that data-stack survey out of the box in future events. The polling changes mean the page stops nagging the server when the tab isn't visible or the browser is unfocused — so fewer useless requests.
Maya: And the login/session work is nice: instead of relying on a client-side token only, the front end exchanges the Google credential for a short-lived session token signed by the server. Anand stores a small local secret file so sessions survive server restarts — that keeps people signed in for a day rather than bouncing them out every half hour.

Alex: He also added per-question counts on the server side and caches them, then surfaces them in the UI. That means attendees see live totals of how many verified users answered each question without the server doing an expensive recount on every poll.
Maya: Non-obvious takeaway: a tiny UX improvement — pausing polling when unfocused — can cut server load dramatically and extend battery life on attendees' devices. Also, using a local session-secret file with careful file permissions is a pragmatic way to persist signing keys across restarts without complex key management.
Alex: Quick idea: if you run any in-room live app, mirror the focus-aware polling pattern and keep a small server-side cache for counts rather than recomputing them every time.

Maya: Next up: Anand's talks publishing work. He pushed a lot of content from recent workshops — full web pages, transcripts, prompts, and post-event writeups for a couple of mid-June talks.
Alex: Practically, that means recordings, transcripts, slide decks and automated analyses are now published and linkable. He also included survey analyses where he fed audience responses into an agent and published the agent's insights as shareable pages.

Maya: Why this matters: converting a live talk into a set of re-usable artifacts — transcript, story, a short summary and analysis — makes the knowledge easy to reuse and to iterate on. The agent-assisted analysis is a neat trick: ask the audience, feed the answers to an LLM that applies a data-analysis checklist, and you get a readable synthesis in minutes.
Alex: Non-obvious takeaway: if you run live sessions, capture the poll data and have an agent produce one short “what to remember” paragraph right after the session. It closes the loop and creates a durable artifact.

Maya: On the blog side, Anand added a set of "writing lenses" — short, reusable steering instructions to shape how an AI answers. Names like “evidence-provenance,” “demo-artifact-first,” “failure-redteam,” and “workflow-to-system.”
Alex: Those are neat: they’re effectively composable modifiers that tell an assistant how to prioritize verification, produce a demo first, probe for failure modes, or turn a repeated task into a small reusable system. Practically, they let Anand and others get more consistent, trustworthy, and actionable outputs from LLMs.

Maya: The practical idea here is to treat these lenses as building blocks: combine “demo-artifact-first” and “evidence-provenance” when you want a reproducible demo with citations. Compose “decision-compression” when you need a clear recommendation at the top. It’s a lightweight alternative to hard-coding behavior in every prompt.
Alex: Non-obvious: having a library of short, named lenses scales better than long one-off prompts — it's easier to reason about and to teach others to reuse.

Maya: Tools got an important fix this week — the ChatGPT scraper he uses to archive conversations. Anand improved attachment detection and the way the scraper captures the model's internal “thinking” panels.
Alex: Practically, the scraper will now include uploaded files and the model’s reasoning details in the exported markdown. That makes saved conversations far more useful for later audits or reproducing a demo, because you no longer lose attachments or the intermediate reasoning that led to an answer.

Maya: Non-obvious takeaway: when scraping modern web UIs, don't rely only on CSS classes — they change. Look for semantics: roles, aria labels, and top-level groupings. Also snapshot any pop-up reasoning panels and attachments as standalone markdown blocks so your archive remains self-contained.

Alex: Anand also updated his LLM math benchmark and the automation around it. He added newer models, made the web table sortable by date, and put in a workflow so new models can be evaluated incrementally without re-running everything.
Maya: Small but crucial notes: he fixed a JavaScript rounding gotcha by storing the largest expected integer as a string, and he added a resume/merge pattern so partial runs from PromptFoo are safely merged. That saves API cost and reduces reruns.

Alex: For folks running numerical tests in JS: if you deal with very large integers, store them as strings or use a big-int-aware path to avoid silent rounding errors. Practical tip: incremental evaluation and merging is how you keep long-running benchmarks affordable.

Maya: On the pricing and frontier tracking side, Anand added a CDP-based downloader and a shell wrapper so he can scrape LMArena leaderboards using a browser with the DevTools protocol. It automates updating his cost-vs-quality frontier chart.
Alex: The takeaway: when no clean API exists, automating a headless browser against a local CDP endpoint can be a reliable stopgap. He also noted parts of the old billing script are now historical — a good reminder to mark deprecations in docs.

Maya: A couple of other housekeeping things: the weekly notes and trending repos were refreshed, and the small prompts repo was migrated into the blog. Nothing flashy, but it keeps the knowledge graph tidy.

Alex: Time for listener tips. I'll go first: if you run any realtime app that polls, stop polling when the page is hidden or the tab loses focus, and do an immediate refresh when the user comes back if it's been a few seconds. That saves server cost and gives a smoother UX. Maya — how would you apply that?
Maya: I’d use it in any dashboard I run for client demos. Pause the polling when the presenter tabs away, and on focus, ask the server for deltas and update the UI. Saves bandwidth during long demos and avoids confusing stale numbers.

Alex: My second tip — persist short-lived session secrets locally (with strict permissions) so a local dev server can survive restarts and keep users logged in for a reasonable time. Maya — your use?
Maya: For internal tools I maintain, I’d create a small session-secret file like Anand did, chmod it to user-only, and use it to sign session tokens. It makes workshop demos less fragile and avoids reauth churn.

Alex: Your tip, Maya?
Maya: Make small, named prompt lenses for recurring behaviours — say “evidence-provenance” or “demo-first” — and compose them instead of writing giant prompts each time. I’ll try this when I ask an assistant to draft product specs: apply “decision-compression” plus “demo-artifact-first” so I get a recommendation plus a tiny runnable example.

Maya: One more from me — when archiving chats, capture attachments and any reasoning panels as discrete markdown blocks. That way, someone reading the archive later has both the files and the rationale. I’ll add that to my research notebook export flow.

Alex: Great — thanks, Maya. And thanks to Anand for another week of practical improvements: smoother live workshops, more reusable publishing artifacts, sturdier scrapers, and better benchmarks.
Maya: If you liked this rundown, try one of the tips this week — especially the polling trick. We'll be back next week with more code stories from Anand's repos.
Alex: Goodbye for the week!
Maya: Bye everyone — see you next Monday.