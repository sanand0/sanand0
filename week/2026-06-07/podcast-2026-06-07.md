Alex: Hello and welcome to Anand's Weekly Codecast for the week of 07 June 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Let's start with the biggest chunk — Anand's personal blog. He added a string of essays and refreshed a bunch of prompts and metadata. What stood out to you, Maya?

Maya: Three things. First, a long-form essay about how AI bottlenecks keep shifting — across coding, agents, enterprise knowledge work, and document processing. Second, a collection of short, practical posts: one about a tiny travel hack, one about a prompt that ended up being longer than the generated HTML, and a new framework piece called "No juniors, no experts?" about how to preserve skill development as AI eats entry-level work. And third, he updated prompt fragments and meeting-prep guidance to match his current accounts and data locations.

Alex: Why does that matter beyond being a good read?

Maya: The essay is useful because it reframes progress. Instead of asking whether AI is "done," Anand points out the bottleneck moves — from raw capability to context, to evaluation, to long-horizon reliability. Practically, that means teams should stop treating model accuracy as the only metric and start investing in context tooling, eval pipelines, and human review where it matters. The "No juniors" piece is a concrete playbook: for low-risk work, switch it to system design; for high-risk work, enforce practice with high-fidelity simulators; and for intrinsically rewarding tasks, level up with the machine as coach.

Alex: Any non-obvious takeaways?

Maya: One: document your assumptions and how they change. Anand published a "my changing AI opinions" post — that kind of record helps you notice when a previously good rule stops applying. Two: small content changes matter for discoverability — he added descriptions and keywords to improve previews and search. Three: keep prompt libraries aligned to the accounts and folders you actually use; a broken path to a data store is a silent failure.

Alex: Good notes. Moving on — Anand spent a lot of time on a small Dockerized static-site gateway that adds Google login and per-folder ACLs. He expanded and then simplified it. Maya, can you explain the change?

Maya: Sure. He began with a minimal, deployable static-pages image that uses nginx and Pomerium for Google OIDC login, plus a small generator that builds Pomerium routes from folder-local .access.yaml files. Then he simplified the secure setup so nginx now fronts Pomerium on the edge, added a combined secure nginx config, improved the ACL compiler script (moved to a uv/pyproject-style header and used PyYAML), added a visible logout link on the demo site, and updated docs to use example.com in examples.

Alex: What practical problems does this solve?

Maya: A few. One: the nginx/Pomerium arrangement now handles Cloudflare-style rewrites like /.pomerium/callback/index.html cleanly, avoiding a common 404 on redirects. Two: the compile-acl script is more maintainable and safer — it rejects unsupported patterns and generates a clear policy. Three: the README now explains why a data/databroker directory appears (runtime state files), how to safely remove it, and alternatives like named volumes or non-root containers. Those are the kind of operational details that trip people up.

Alex: Any gotchas to watch for?

Maya: Yes — don't use the same hostname for the protected app and the authenticate service. And when a runtime RocksDB-style folder appears, stop the container before deleting it; otherwise you’ll corrupt runtime state. If host-owned root:root files are inconvenient, use a Docker named volume or change container UID after making runtime paths writable.

Alex: Great practical stuff. The next big area was Anand's scripts repository — the personal utilities and activity recorder. Lots of edits there.

Maya: Right. He expanded the daily activity generator to include personal email, made reports patchable so you can refresh specific sources later, and wired backups and scanning helpers into workflows. He also improved Whatsapp backup scanning to tolerate slightly out-of-order chats, and tweaked the call-transcribe flow to process prompted .opus files newest-first. Plus a bunch of developer tooling: safer dev Docker, cloudflare tunnel start/stop integration, new helper commands and agent skills.

Alex: Why do those changes matter in day-to-day use?

Maya: Two big practical wins. First, including personal email and making sources patchable keeps activity logs complete without mixing credentials — that matters for truthful daily notes and automated summaries. Second, robustness improvements — incremental scanning that tolerates messy timestamps and processing newest files first — reduce missed items and make reruns safe. Those are small changes that prevent chronic loss of data or tedious reruns.

Alex: Any practical ideas to borrow?

Maya: Set mtimes of generated daily reports to the midnight of the day they describe — Anand adopted this. It helps file sorting and backups behave predictably. Also, prefer designing patching mechanisms so you can re-run only the flaky sources instead of rebuilding everything.

Alex: Now to Anand’s little web tools — two highlights: a color-table generator and a LinkedIn invite scraper. What changed?

Maya: The color-table tool got several UX and parsing upgrades: it now supports Markdown tables, recognizes consistent currency prefixes/suffixes when scaling numbers, adds a "reverse color scale" option, and exposes the full set of D3 continuous color scales grouped by type. Tests were added first, which is lovely.

Alex: Why does that matter for people who make charts?

Maya: Twofold: more forgiving input (Markdown and currency-aware parsing) means designers and analysts can paste source material without pre-cleaning. Reverse-scale is a tiny but often necessary visual tweak. And grouping color scales by type helps pick perceptually appropriate palettes for continuous vs diverging data.

Alex: The LinkedIn invite scraper?

Maya: Anand fixed brittle parsing where taglines and UI text like "wants to connect" leaked into the description and common organization fields. He added a more robust element-based text extraction function, heuristics to ignore profile-photo noise, and tests for tricky HTML. That makes the scraped summaries actually useful for triage.

Alex: Non-obvious lesson?

Maya: Real-world HTML is messy. Defensive parsers and tests that capture common UI quirks save hours. Always add tests for the broken cases you see in the wild.

Alex: Anand also pushed a new research project: an LLM vs Stockfish chess evaluation harness. Tell us about that.

Maya: He built a reproducible CLI to play an LLM against Elo-limited Stockfish, log moves and API usage, and produce PGN and JSONL artifacts. There are scripts to sweep model/reasoning/Elo combinations adaptively, and tests/build tooling to summarize results. It's aimed at measuring both competence and token costs.

Alex: Why is that useful beyond chess?

Maya: It's a model evaluation pattern: define a strict action schema (legal moves), limit an oracle opponent, log structured interactions, and sweep parameters adaptively. That gives quantitative tradeoffs: model quality vs token cost vs reasoning settings. You can reuse the pattern for other decision tasks where correctness is formalizable.

Alex: Quick note — Anand also updated the weekly group podcast automator and the "Things I learned" notes, plus added a download button for SVGs in his journalists project. Any pick from those?

Maya: The podcast update itself included a tidy weekly digest script and a practical tip — audit token sinks and wire a single "system truth" file into your agent so it consistently uses product rules. The TIL update refreshed trending repos, giving good ecosystem signals. The journalists change adding an SVG download button is small but directly improves usability for folks who share the visual tips — a reminder that little UX fixes matter.

Alex: He also trimmed the model list in the art-style site to the latest models — small housekeeping, but it keeps examples up-to-date.

Maya: Yes, keeping example configs current prevents confusing users when older models are deprecated or replaced. It's a maintenance win.

Alex: Okay, listener tips time. I'll go first: audit token sinks in any agent loop you run this week. Find one loop that repeatedly re-sends the same heavy context or verbose reasoning, and try a cache — either stable history or a cheaper model for recall — then measure token usage before/after. Maya, how would you apply that?

Maya: I'd pick the one integration that handles meeting transcripts — swap repeated transcript lookups into a small cached summary file the agent reads, instead of streaming full transcripts every time. For me, that should cut tokens and keep context freshness.

Maya: My tip: when you add auth proxies or runtime state to a container, document where runtime files live and how to delete them safely. If you use an on-disk databroker like RocksDB, always stop the container before removing the folder, or use a named volume. Alex, how would you apply that?

Alex: I’d add a short README snippet near the compose file and put a tiny script: "stop -> backup data/ -> rm -rf data/databroker -> up". That way it's a safe, repeatable workflow people won't forget.

Maya: Those are practical and small — perfect.

Alex: That wraps our rundown. Thanks for walking through these changes with me, Maya.

Maya: Thanks, Alex. And thanks to Anand for documenting so much — makes it easy for us to unpack and share.

Alex: Goodbye for the week — see you next time on Anand's Weekly Codecast.

Maya: Bye everyone — keep building, measuring, and documenting. See you next week.