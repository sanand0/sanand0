Alex: Hello and welcome to Anand's Weekly Codecast for the week of 26 July 2026!
Maya: We're Alex and Maya, and today we'll walk you through the highlights of Anand's commits this week.

Alex: Let's dive into the biggest story: Anand shipped a polished release and a reproducible demo package for his browser tab manager with notes — the little extension that turns your open tabs into a searchable side-panel with notes, tags, and durable records.
Maya: This was a big engineering and documentation push. He fixed a real crash/restart edge case, bumped the release, and also created a full pipeline to capture reproducible product screenshots and a short Build Week video using synthetic demo data and Playwright. Practical effect: the extension is safer after crashes, the release is verifiable, and anyone can reproduce the demo without leaking private data.
Alex: Can you say more about the crash fix in plain terms?
Maya: Sure — browsers sometimes restore tabs on restart in a way that races with the extension's "tab closed" handlers. Anand added logic that converges those close actions reliably: if you intentionally marked a record to be retained, it won't be lost just because the browser restored the tab a bit later. He also added a UI button — "Recover restored tabs" — that runs an explicit reconciliation step after the browser finishes restoring tabs, and the side-panel reports how many matches it recovered and how many remain ambiguous.
Alex: Why does that matter for everyday users?
Maya: Because the prior behavior could silently attach notes to the wrong tab or drop context. Now the extension refuses to guess when evidence is ambiguous and makes uncertainty visible. Visible uncertainty beats silent corruption — you'll know when a record needs human attention rather than having the wrong note silently glued to a page.
Alex: The demo and video work looked massive. What did Anand do there?
Maya: He built a reproducible asset pipeline: synthetic local pages and a validated JSON fixture, scripts to load the exact release into an isolated Chromium profile via Playwright, automated captures of product scenes, TTS narration clips, composition scripts for gallery artboards, and audits that check images for PII or unwanted metadata. He also documented the exact prepare/rehearse/record/cleanup steps so someone else can run the same flow and produce the same judge-ready MP4.
Alex: Non-obvious takeaway?
Maya: Two things. First, recording the real product in an isolated profile catches real bugs that unit tests and headless scripts can miss — that's exactly where the close-vs-refresh race came from. Second, make your demo reproducible and auditable: synthetic fixtures + an automated PII audit make a lot of people comfortable publishing screenshots and videos from private software.
Alex: Practical idea: if you demo an extension or any UI that uses a browser profile, spin up an isolated profile, use synthetic data, and script the whole capture — not just a manual screen recording. That way you can re-run captures, test fixes, and avoid leaking private info.
Maya: How would you apply that, Alex?
Alex: I'd start with a small prepare script that verifies the release artifact checksum, creates a disposable profile, loads the extension, imports a synthetic fixture, and then records a set of deterministic interactions. One script, one command to reproduce the demo.

Maya: Next up: Anand's personal scripts repo got a lot of attention this week. There are three big things: a brand new mail indexing tool, transcript summarizer improvements, and a few tidy maintenance fixes for music-tagging and productivity snippets.
Alex: Tell me about the mail indexer first.
Maya: Anand added a mail-indexing script that builds a compact, resumable SQLite full-text index over local mbox files. That means you can search decades of archived mail quickly without loading everything into memory — SQLite FTS is the search engine, resumability means the index can be updated incrementally, and the tool is explicitly designed to be robust for offline workflow. Practical effect: fast local search over old email archives without sending anything to a server.
Alex: And the transcript summarizer changes?
Maya: He introduced a conservative "what-i-missed" field in meeting transcripts — a focused prompt that looks for moments where Anand (the speaker) actually missed or under-reacted to someone else's request or signal. Importantly, the code:
- removes old AI-generated fields before re-running the model to avoid anchoring,
- normalizes a small taxonomy of reasons (e.g., "topic shift"),
- adds tests that assert conservative behavior (often empty) and that later conversation turns are re-checked so we don't falsely flag resolved items.
Practical effect: more reliable meeting notes that surface only unresolved, high-leverage misses and avoid noisy or trivial "missed" items.
Alex: That's a neat design pattern — avoid reusing previous AI outputs as context when you want a fresh judgment.
Maya: Exactly. The code even includes test suites for the prompt behavior, which is crucial when you iterate on prompts and model versions.
Alex: What about the music-tagging and other small fixes?
Maya: He cleaned up the music-tag tool to warn that fixes aren't applied unless you pass --write and to delete legacy APEv2 tags when writing new metadata. Also updates to espanso (text expansions), a small improvement in the dev dockerfile, and more guidance in his mcpserver notes. Little changes, but they improve correctness and UX.
Alex: Non-obvious takeaway?
Maya: When tools edit files or metadata, be explicit about "preview vs apply" and clean up legacy metadata that can otherwise confuse downstream tools. And always add tests around the actual side-effects — file writes, tag deletion — not just the in-memory logic.

Alex: Moving to Anand's blog and writing/tooling changes: he migrated several internal "skills" into user-facing prompts, added a new talk-prep workflow, and published a few posts and TIL notes.
Maya: The core themes here are making agent workflows explicit and adding a verification/guardrail layer. He added a "verification gate" skill — basically a checklist: scope the pass, reconstruct the spec, decompose into checkable units, get independent signals (re-derive, don't just re-read), hunt known failure modes, and fix only identified defects. Practical effect: agents and humans following this help avoid overconfident, unverifiable outputs.
Alex: He also wrote that reframe-question skill, right?
Maya: Yes — it's about treating the user's question as a draft of their real need. The technique: infer the decision, test whether reframing changes what's being solved, reframe visibly with one-line reason, then answer. And he included eval cases to detect unnecessary reframes or invented intent. It's a small but powerful habit for being useful rather than clever.
Alex: Non-obvious takeaway?
Maya: Codify these meta-skills. When agents or teammates can run the same verification and reframing steps, you scale reliability. Also, move operational skills (like "email-reply" or "talk-prep") into reproducible prompts with explicit inputs, sources to consult, and expected outputs — that makes them versionable and testable.
Maya: He also posted a couple of essays and updated TILs — good reading if you care about how to use AI in teams and what skills will matter.

Alex: Short hits from Anand's smaller projects: in his collection of tiny web tools, he added themes for a slide editor and a small Gmail bookmarklet pack.
Maya: The slide editor work makes the "Edit Slide" form more compact, adds independent font controls for title and subtitle, and ships pre-designed themes you can pick from a dropdown. That means quicker, prettier slides from the browser. The Gmail bookmarklet copies expanded "From" lines and the Subject, and shows a small transient notification when it copies — handy little productivity utilities.
Alex: Non-obvious takeaway?
Maya: Small UX details matter: putting color pickers, font selectors, and range inputs on single lines makes a big difference to perceived polish. And for browser bookmarklets, test them against a static fixture page and add small visual feedback so users know it worked.

Maya: A few other quick notes: Anand deployed his research site to GitHub Pages and tweaked an LLM-writing-style demo (added copy feedback and a small copy button), and he updated model data in his LLM pricing tool so the frontier chart reflects newer leaderboards.
Alex: And his weekly notes were updated with new "things I learned" and a refreshed trending-repos snapshot. Small maintenance, but useful for readers and for keeping his personal knowledge base accurate.

Maya: Okay, listener tip time. Quick, actionable tip from the demo and release work?
Alex: If you ever need to demo software that touches user data, build a reproducible isolated demo pipeline: one script to verify the release, then an automated profile setup that loads only synthetic data, runs deterministic interactions, captures screenshots/video, and runs a PII audit. This prevents accidental leaks and makes your demo reproducible. Maya, how would you apply that this week?
Maya: I'd use it next time I show a live dashboard: spin up a disposable profile, seed it with synthetic rows representing plausible cases, and script a short set of interactions so I can iterate on narration and fix bugs before a live demo.

Maya: My tip from the transcript and prompt work: when you regenerate AI outputs (meeting summaries, annotations), strip the old AI-generated fields before calling the model again so prior output doesn't anchor the new result. Then keep the "what-i-missed" prompt narrow and conservative — it's better to be empty than noisy. Alex, how would you use that?
Alex: I'll add a small wrapper around our weekly-report generator that removes prior "AI-suggested" sections before re-running the summarizer. And I'll add a short test that asserts the regenerated "what-i-missed" is either empty or contains concrete evidence and a suggested next move.

Alex: That's all for this week. Maya, any final notes?
Maya: Hats off to Anand for turning a demo into reproducible evidence and for tightening the human+agent workflows. Small UX changes, conservative verification, and reproducible demos are the things that scale trust.

Alex: Thanks for listening. Have a great week, and we'll see you next time on Anand's Weekly Codecast.
Maya: Bye!